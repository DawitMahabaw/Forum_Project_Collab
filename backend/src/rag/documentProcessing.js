// Kept as a stable import path for code written before the RAG service was
// consolidated. The implementation lives in ragService.js.
export {
  createDocument,
  extractText,
  processDocument,
} from "./ragService.js";
